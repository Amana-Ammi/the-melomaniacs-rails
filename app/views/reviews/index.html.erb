<h1>ALL REVIEWS </h1>

<%= @reviews.each do |review| %>

    <%= review.title %>
    <%= review.content %>
    
    <%# <%= link_to review.user.username, user_path%> %>
    <%= button_to "DELETE REVIEW", review_path(@review.id), method: :delete %>
    
<% end %>