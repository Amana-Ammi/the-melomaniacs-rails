<h1>NEW REACTION for <%= @reaction.review.user.username.capitalize%>'s <%= @reaction.review.title %> Review </h1>

<%= form_for(@reaction) do |f|%>
<div>
<%= f.label :comment %>
<%= f.text_area :comment %>
</div>
<%= f.submit %>
<% end %>